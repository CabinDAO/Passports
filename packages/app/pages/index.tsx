import type { NextPage } from "next/types";
import Layout from "../components/Layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <div />
    </Layout>
  );
};

export default Home;
